#
# Development/test dependency pins for this repository.
#
# This branch targets SQLAlchemy 2.0.50 and validates behavior using the
# SQLAlchemy dialect testing suite (pytest plugin).
#
# Recommended environment:
# - Python: 3.11.x
# - SQLAlchemy: 2.0.50
# - pytest: 7.x or newer
# - YashanDB Python DBAPI: yaspy==1.2.1 (required for DB-backed tests)
#
# yaspy:
#   Pinned to 1.2.1 to match README-USER-GUIDE.md and tested behavior
#   (BLOB NUL round-trip, RETURNING, etc.). If using a private index,
#   pass -i/--extra-index-url or set pip.conf.
#
# Install:
#   pip install -r requirements_dev.txt
#

SQLAlchemy==2.0.50
pytest>=7.0.0,<10
yaspy==1.2.1

