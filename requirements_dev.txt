#
# Development/test dependency pins for this repository.
#
# This project targets SQLAlchemy 1.4.5 and validates behavior using the
# SQLAlchemy dialect testing suite (pytest plugin).
#
# Recommended environment:
# - Python: 3.9.x
# - SQLAlchemy: 1.4.5
# - pytest: 6.2.5
# - YashanDB Python DBAPI: yaspy (listed below; required for DB-backed tests)
#
# yaspy:
#   Published on PyPI (or your team index). Listed without a version pin so
#   `pip install -r requirements_dev.txt` installs the latest compatible release
#   from the configured index. For bit-for-bit reproducible CI, pin explicitly, e.g.:
#     yaspy==1.2.3
#   If using a private index, pass -i/--extra-index-url or set pip.conf.
#
# Install:
#   pip install -r requirements_dev.txt
#

SQLAlchemy==1.4.5
pytest==6.2.5
py==1.11.0
pluggy==1.6.0
yaspy

